program test_pointer {

	int y;
	y = 10;
    int x = 5;
    
    int c = y * x; //multiplicacao
    print(c);
    
    c = y / x; //divisao
    print(c);
   
    c = y + x; //soma  
    print(c);
    
     c = y - x; //subtracao
     print(c);
}